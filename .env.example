# Este é um arquivo de exemplo. Copie este arquivo para um novo arquivo chamado .env
# e preencha com suas próprias credenciais. O arquivo .env não deve ser versionado.

# --- Configurações do Banco de Dados PostgreSQL ---
# Estas variáveis são usadas pelo Docker Compose para inicializar o contêiner do banco de dados.
POSTGRES_DB=synapse_db
POSTGRES_USER=synapse_user
POSTGRES_PASSWORD=coloque_uma_senha_forte_aqui

# --- Configurações da Aplicação Flask ---
# Chave secreta usada para assinar os tokens de autenticação JWT.
# Deve ser uma string longa, aleatória e segura.
JWT_SECRET_KEY="coloque_uma_chave_secreta_forte_e_aleatoria_aqui"

# Chave de API para o serviço GNews.
GNEWS_API_KEY="sua_chave_da_gnews_api_aqui"

# Variáveis padrão do Flask
FLASK_APP=backend.app.main

# --- URL de Conexão com o Banco de Dados ---
# Esta URL é usada pelo SQLAlchemy dentro do contêiner do back-end para se conectar ao contêiner do banco.
# O nome 'db' se refere ao nome do serviço do banco de dados no arquivo docker-compose.yml.
# Certifique-se de que o usuário, senha e nome do banco correspondam aos valores definidos acima.
DATABASE_URL=postgresql+psycopg://synapse_user:strong_password@db:5432/synapse_db